import { Canvas, Meta, Story, ArgsTable, Controls } from '@storybook/blocks'
import Alert from '@turistikrota/ui/alert'
import * as AlertStories from './Alert.stories'

<Meta of={AlertStories} />

# Alert

```jsx
import Alert from '@turistikrota/ui/alert'
```

Alert component is used to display a short, important message in a way that attracts the user's attention without interrupting the user's task.

## Alert Variants

Alerts are available in six variants: primary, secondary, success, error, info and warning.

<Canvas of={AlertStories.Playground} />

<Controls />

## Alert with icon

<Canvas of={AlertStories.ShowIcon} />

## Alert with close button

<Canvas of={AlertStories.Closable} />

## Alert with description

<Canvas of={AlertStories.Description} />

## Alert with title

<Canvas of={AlertStories.Title} />

## Alert with title and description

<Canvas of={AlertStories.TitleAndDescription} />

## Alert with full options

<Canvas of={AlertStories.Full} />
