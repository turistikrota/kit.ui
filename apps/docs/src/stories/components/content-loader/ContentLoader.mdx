import { Canvas, Meta, Story, ArgsTable, Controls } from '@storybook/blocks'
import * as ContentLoaderStories from './ContentLoader.stories'
import 'sspin/dist/index.css'

<Meta of={ContentLoaderStories} />

# ContentLoader

ContentLoader component is used for displaying a placeholder content skeleton before the data gets loaded to reduce the load time perceived by the user.

<Canvas>
  <Story of={ContentLoaderStories.Playground} />
</Canvas>
