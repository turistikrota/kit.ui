import { Canvas, Meta, Story, ArgsTable, Controls } from '@storybook/blocks'
import * as LineStories from './Line.stories'

<Meta of={LineStories} />

# Line Form

```jsx
import LineForm from '@turistikrota/ui/form/line'
```

The Line form is used to create a row of fields.

<Canvas of={LineStories.ExampleUsage}>
  <Story of={LineStories.ExampleUsage} />
</Canvas>
