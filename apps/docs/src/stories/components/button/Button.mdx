import { Canvas, Meta, Story, ArgsTable, Controls } from '@storybook/blocks'
import * as ButtonStories from './Button.stories'

<Meta of={ButtonStories} />

# Button

Button component with different variants and sizes. It can be used for actions, like in this example, or for toggling states.

<Canvas>
  <Story of={ButtonStories.Playground} />
</Canvas>

<Controls />
